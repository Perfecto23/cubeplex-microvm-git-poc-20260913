# AgentCore MicroVM Git fixture

This public repository contains only synthetic test code.

Task: fix inclusive_total so both endpoints are included. Preserve the existing validation and make all unittest cases pass. The cloud Agent must inspect the source and tests, produce its own commit, push the assigned task branch, and open a pull request. Do not merge the PR.

Run: python -m unittest -v test_intervals.py

The initial implementation is intentionally incorrect.
